import Layout from "@/components/Layout";

export default function ExportPage() {
  return (
    <Layout>
      <div className="card p-6">
        <h1 className="text-lg font-semibold">数据导出</h1>
        <p className="text-text-secondary mt-2">按日/周/月与按班级导出（占位）</p>
      </div>
    </Layout>
  );
}

